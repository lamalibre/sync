name: Desktop Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 0.1.0)'
        required: true
      prerelease:
        description: 'Mark as pre-release'
        type: boolean
        default: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    outputs:
      tag: desktop-v${{ github.event.inputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Create release tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "desktop-v${{ github.event.inputs.version }}"
          git push origin "desktop-v${{ github.event.inputs.version }}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: desktop-v${{ github.event.inputs.version }}
          name: Sync Desktop v${{ github.event.inputs.version }}
          prerelease: ${{ github.event.inputs.prerelease }}
          body: |
            ## Sync Desktop v${{ github.event.inputs.version }}

            ### Installation

            Download the appropriate file for your platform:

            | Platform | File |
            |----------|------|
            | macOS Apple Silicon (M1/M2/M3/M4) | `Sync_${{ github.event.inputs.version }}_aarch64.dmg` |
            | macOS Intel | `Sync_${{ github.event.inputs.version }}_x64.dmg` |
            | Linux x64 (Debian/Ubuntu) | `sync_${{ github.event.inputs.version }}_amd64.deb` |
            | Linux x64 (AppImage) | `sync_${{ github.event.inputs.version }}_amd64.AppImage` |
            | Linux arm64 (Debian/Ubuntu) | `sync_${{ github.event.inputs.version }}_arm64.deb` |
            | Linux arm64 (AppImage) | `sync_${{ github.event.inputs.version }}_aarch64.AppImage` |

            > **macOS note:** The app is not code-signed. On first launch, right-click → Open, or go to System Settings → Privacy & Security → Open Anyway.

  build:
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            runner: macos-latest
          - target: x86_64-apple-darwin
            runner: macos-latest
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-22.04
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-22.04-arm

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: packages/sync-desktop/src-tauri

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            xdg-utils

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tauri app
        working-directory: packages/sync-desktop
        run: pnpm tauri build --target ${{ matrix.target }}

      - name: Upload macOS .dmg to release
        if: runner.os == 'macOS'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: desktop-v${{ github.event.inputs.version }}
          files: |
            packages/sync-desktop/src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg

      - name: Upload Linux .deb and .AppImage to release
        if: runner.os == 'Linux'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: desktop-v${{ github.event.inputs.version }}
          files: |
            packages/sync-desktop/src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            packages/sync-desktop/src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
